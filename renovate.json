{
  "$schema": "https://docs.renovatebot.com/renovate-schema.json",
  "extends": ["config:recommended"],
  "description": "Watches the Image= lines in the Quadlet .container files so PRs open automatically when Prometheus/Alertmanager/Grafana/exporter images publish new tags. Inert until the Renovate GitHub App is enabled for this repo.",
  "customManagers": [
    {
      "customType": "regex",
      "description": "Quadlet .container Image= lines",
      "fileMatch": ["\\.container$"],
      "matchStrings": [
        "Image=(?<depName>[^:\\s]+):(?<currentValue>[^\\s]+)"
      ],
      "datasourceTemplate": "docker"
    }
  ]
}
